kind = "web"
previewPath = "/"
title = "JustIdeas - Înregistrare Firme"
version = "1.0.0"
id = "artifacts/justideas-web"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25835

[services.development]
run = "pnpm --filter @workspace/justideas-web run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/justideas-web", "run", "build" ]
publicDir = "artifacts/justideas-web/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25835"
BASE_PATH = "/"
